# .github/workflows/test-shield-encryption.yml
name: Test Redaction Guard

on:
  workflow_dispatch:
    inputs:
      sensitive_data:
        description: 'Sending fake github token / key - redaction guard should redact this'
        required: true
        default: 'ghp_1234567890abcdefghirklmnopqrstuvwxzy12'
        

jobs:

  shield-send-to-zekt:
    name: Send Sensitive (Redacted) Event & message payload to Zekt
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # Required for OIDC authentication
      contents: read
    
    steps:
      - name: Build JSON Payload
        id: build-payload
        shell: pwsh
        run: |
          # Collect all input parameters into a structured JSON object
          Write-Host "Building arbitrary JSON structure"

          $payload = @{
              timestamp = (Get-Date -Format "o")
              workflowRunId = "${{ github.run_id }}"
              repository = "${{ github.repository }}"
              sender = "${{ github.actor }}"
              sensitive_data = "${{ inputs.sensitive_data }}"
          }
          
          Write-Host "Post-collecting input parameters..."
          
          # Convert to JSON string
          $jsonPayload = $payload | ConvertTo-Json -Compress
          
          # Output for next step
          Write-Host "Payload built:"
          Write-Host $jsonPayload
          
          # Set as output (escape for GitHub Actions)
          "payload=$jsonPayload" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
      
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Send Event to Zekt
        id: zekt
        uses: zekt-dev-org/zekt-action@v2.1.0
        with:
          event-type: 'zekt-custom-redaction-provider-event'
          payload: ${{ steps.build-payload.outputs.payload }}
          shield: false

      - name: Confirmation
        run: |
          echo "✅ Event sent to Zekt successfully!"
          echo ""
          echo "📋 Summary:"
          echo "  - Event Type: zekt-custom-redaction-provider-event"
          echo "  - Event ID: ${{ steps.zekt.outputs.event-id }}"
          echo "  - Status: ${{ steps.zekt.outputs.status }}"
          echo "  - Consumers Notified: ${{ steps.zekt.outputs.consumers-notified }}"
          echo "  - Workflow Run: ${{ github.run_id }}"
          echo "  - Repository: ${{ github.repository }}"
          echo "  - Triggered by: ${{ github.actor }}"
          echo ""
          echo "🔗 Next steps:"
          echo "  1. Check Zekt payload is redacted"
          echo "  2. Verify event was received and decrypted"
          echo "  3. Check if consumers received the forwarded event"
          echo "  4. Check that consumer workflow triggers on custom event type"
